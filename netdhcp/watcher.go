package netdhcp

/*
// WatcherOptions define the options for watched values.
type WatcherOptions struct {
}

// GlobalWatcher is an interface for monitoring global configuration.
type GlobalWatcher interface {
	Next(context.Context) (*Global, error)
}

// InstanceWatcher is an interface for monitoring instance configuration.
type InstanceWatcher interface {
	Next(context.Context) (*Instance, error)
}

// NetworkWatcher is an interface for monitoring network configuration.
type NetworkWatcher interface {
	Next(context.Context) (*Network, error)
}
*/
